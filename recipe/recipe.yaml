schema_version: 1

context:
  version: "0.6.20260414"

package:
  name: ibm-quantum-schemas
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/i/ibm-quantum-schemas/ibm_quantum_schemas-${{ version }}.tar.gz
  sha256: a994d92ccd3ef7f3ee4358ec13e1ce18a52f1b67c4a0c37528e19e913341f635

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools
    - setuptools-scm
    - pip
  run:
    - python >=${{ python_min }}
    - pybase64 >=1.3.0
    - pydantic >=2.11,<3.0.0
    - qiskit >=2.2.0,<3.0.0
    - samplomatic >=0.12.0

tests:
  - python:
      imports:
        - ibm_quantum_schemas
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  summary: IBM Quantum Pydantic models.
  license: Apache-2.0
  license_file: LICENSE.txt
  homepage: https://github.com/Qiskit/ibm-quantum-schemas

extra:
  recipe-maintainers:
    - wshanks
